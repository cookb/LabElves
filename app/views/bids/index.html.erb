<h3 style="color:green">All Current Bids For This Task</h3>

<ul>
	<% @task.bids.each do |bid| %>
		<li><%= link_to "#{bid.elf.name}, #{bid.time_bid.to_date}, 
			$#{bid.credits_bid}", bid_url(bid) %></li>	
		<br>
	<% end %>
</ul>

<%= link_to "Back to Task", task_url(@task) %>
