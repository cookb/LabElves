<!DOCTYPE html>
<html>
<head>
  <title>LabElves</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

	<!-- <div>
	<% if flash[:notice] %>
		<p><%= flash[:notice] %></p>
	<% end %>
	</div> -->

	<p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
	
</head>
<body>
	
<% if current_user %>
<ul>
	<b>Logged in as: <%= current_user.email %></b>
  <%= button_to "Logout", destroy_user_session_url, :method => :delete %>
	<% if request.original_url != root_url %>
	<%= button_to "Home", root_url, :method => :get %>
	<% end %>
</ul>
<% end %>

<%= yield %>

<img src="/assets/WillFerrellElf.jpg" class="Elf">

</body>
</html>
