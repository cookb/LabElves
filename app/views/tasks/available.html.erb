<h3 style="color:green">List of all currently available Tasks</h3>
<%= render partial: "list", locals: { tasks: @tasks } %>

<%= button_to "Back to Elf Task History", performed_tasks_url, :method => :get, :class => "btn btn-large btn-inverse" %>